// Global default params, used in configs
params {

    // Input options
    dbname                     = "swissprot"
    fasta                      = "${baseDir}/dataset/aMicUni.selected_proteins.fa.gz"
    gff                        = null
}
